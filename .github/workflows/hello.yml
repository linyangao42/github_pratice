name: Hello, World!
on: push

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World!"
      - run: echo $BRANCH_NAME
        env:
          BRANCH_NAME: ${{github.head_ref}}
