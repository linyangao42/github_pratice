name: Hello, World!
on: push

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World!"
      - run: echo ${GITHUB_REF#refs/heads/}
        #env:
        #  BRANCH_NAME: ${{github.head_ref}}
