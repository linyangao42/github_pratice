name: checkout-workfile
on:
  pull_request:
    branches:
      - main
    #paths:
     # .github/workflows/**
  
jobs:
  checkout-job:
    name: pull-request-20220113
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: echo "hello"
