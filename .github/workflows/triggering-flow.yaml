name: Trigger
on:
  push:
    branches:
      - workflow-run-test
jobs:
  tirgger-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "trigger flow"
      - run: echo "branch"
        if: github.ref == 'refs/heads/test99'
